[project]
name = "dexcom-cli"
version = "0.1.0"
description = "A CLI for working with Dexcom CGM data directly from your terminal."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydexcom>=0.5.1",
    "python-dotenv>=1.2.2",
]
